# This is not used by this repo itself, but for the client which will be connectd to this MCP server

# Project Preferences

## Test Status Reports

When showing test status, use a table format with:
- Summary line showing passing/failing counts
- Table with columns: Tab, Status (with checkmark/X emoji), Passed, Failed, Total
- List of failing tabs with actual test failures at the bottom
- Offer to investigate failures