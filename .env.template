DEFAULT_DASHBOARD=sig-windows-signal

# Directory to store cached data
PROJECTS_ROOT=${HOME}/.k8s-test-analyzer/cache

# Port for the FastMCP server (default: 8978)
FASTMCP_PORT=8978

# Comma-separated list of folder names to index under PROJECTS_ROOT
FOLDERS_TO_INDEX=

# Comma-separated list of additional directories to ignore beyond defaults
ADDITIONAL_IGNORE_DIRS=chroma_db,container_cache

# Comma-separated list of additional files to ignore beyond defaults
ADDITIONAL_IGNORE_FILES=

# Scheduled task interval in seconds (default: 3600 = 1 hour, 0 to disable)
SCHEDULE_INTERVAL_SECONDS=3600

# Number of builds to keep per job during cleanup (default: 10, 0 to disable cleanup)
CLEANUP_KEEP_BUILDS=10
